# Photos

One file per golden case, named by the `photo` field in `../golden/<id>.json`.

Empty on purpose. The 46 photographs that used to be here belonged to the
model-annotated golden set and went with it; the two cases that remain are real
meals with published figures, and their photographs were never committed.

`pnpm eval:nutrition` says which files it is missing and refuses to report an
average over the cases that happened to run.
